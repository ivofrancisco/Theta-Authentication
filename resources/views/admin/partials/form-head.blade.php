<!-- Begin: g-form-head -->
<div class="g-form-head">
    <h4>{{ $title }}</h4>
    @if( $task == 'update' )
        @include('admin.partials.current-languages')
    @endif
</div>
 <!-- End: g-form-head -->